# # This file is generated by Nx.

# # Build the docker image with `npx nx docker-build client`.
# # Tip: Modify "docker-build" options in project.json to change docker build args.

# # Run the container with `docker run -p 3000:3000 -t client`.
# FROM docker.io/node:lts-alpine

# ENV HOST=0.0.0.0
# ENV PORT=3000

# WORKDIR /app

# RUN addgroup --system client && \
#           adduser --system -G client client

# COPY dist/apps/client client/
# RUN chown -R client:client .

# # You can remove this install step if you build with `--bundle` option.
# # The bundled output will include external dependencies.
# # RUN npm --prefix client --omit=dev -f install

# CMD [ "node", "client" ]

FROM nginxinc/nginx-unprivileged

LABEL org.opencontainers.image.title="gc-comp-payouts angular client"
LABEL org.opencontainers.image.url="https://github.com/DouglasWebster/GC-Reports-Server"
LABEL org.opencontainers.image.source=https://github.com/DouglasWebster/GC-Reports-Server
LABEL org.opencontainers.image.description="Angular client for the GC comp payouts application"
LABEL org.opencontainers.image.licenses=MIT

WORKDIR /app

COPY dist/apps/client/browser /usr/share/nginx/html
COPY apps/client/nginx.conf  /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]


